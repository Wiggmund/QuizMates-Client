Session


export interface Session {
  id: number;
  title: string;
  description: string;
  host: number;
  date: Date;
  bestStudent: number; // must be optional
  bestGroup: number; // must be optional
  status: keyof typeof SessionStatus;
}